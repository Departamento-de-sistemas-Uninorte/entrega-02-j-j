# Add your own tweets in files placed in lib/tweets ending in .rake,
# for example lib/tweets/capistrano.rake, and they will automatically be available to Rake.

require_relative "config/application"

Rails.application.load_tasks
